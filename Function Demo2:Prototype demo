#include<stdio.h>
double functionM(void);
int functionN(int n, double x);
double functionO(double, int, double, int);
double functionP(int a, int n, int b, int c);

int main(void) {
  	int a, b, c, d, e ;
  	double r, s, t, u, v;
  	//function calling (a-k)
    functionM();                                //a
    a = functionM();                            //b
    b = functionN(a, b);                        //c
    r = functionO(r,a,s,b);                     //d
    //s = functionP(a,b,c,d,e);                 //e
    u = functionM();                            //f
    c = d + functionN(r,s);                     //g
    t = s * functionO(r, a, r, a);              //h
    //a = v + functionP(r, s, t, t);            //i
    functionP(functionN(a, a), s, t, t+r);      //j
    v = functionP(functionN(a, a), s, t, t+r);  //k
    printf("b) %d\n",a);    
    printf("c) %d\n",b);
    printf("d) %.2f\n",r);
    printf("f) %.2f\n",u);
    printf("g) %d\n",c);
    printf("k) %.2f\n",v);
}
// a) ไม่มีปัญหา เรียกใช้เฉยๆไม่มีอะไรจึงถูก
// b) Run ได้แต่ค่าอาจไม่ตรงตามที่ต้องการ เพราะ functionM ส่งค่ากลับเป็น double แต่ a นั้นเป็น int ค่าทศนิยมที่ส่งกลับจะหายไป
// c) ไม่มีปัญหา แม้ชนิดของ b จะไม่ตรงกับพารามิเตอร์ x แต่ก็ไม่มีปัญหาเนื่องจาก ชนิดของ int b จะกลาย เป็น double แต่สุดท้ายยส่งค่ากลับเป็น int จึงถูกตัดทศนิยมอยู่ดี  
// d) ไม่มีปัญหา ทั้งชนิดตัวแปรและพารามิเตอร์ ส่งค่าคืนตัวแปรก็ถูกต้อง
// e) Error เพราะพารามิเตอร์ของฟังก์ชันมีแค่ แต่ส่งค่าไป 5 ตัว จึง error
// f) ไม่มีปัญหา ส่งค่าคืนเป็น double เก็บลงในตัวแปร double
// g) Run ได้แต่ค่าอาจไม่ตรงตามที่ต้องการ เพราะค่าของ r จะถูกแปลงเป็น int(ตัดทศนิยมออก) ซึ่งอาจจะทำให้ผลลัพธ์คาดเคลื่อนได้
// t) ไม่มีปัญหา ทั้งชนิดตัวแปรและพารามิเตอร์ ส่งค่าคืนตัวแปรก็ถูกต้อง
// i) Run ได้แต่ค่าอาจไม่ตรงตามที่ต้องการ เพราะใส่ชนิดของตัวแปรไม่ตรงกับพารามิเตอร์ค่าจะเพื้ยนได้เพราะตัดทศนิยมออก อีกทั้งยังเอาค่า double ไปเก็บใน int อีก(โดนตัดทศนิยมอีกหลังจากนำมา+กับ v)
// j) Run ได้แต่ค่าอาจไม่ตรงตามที่ต้องการ เพราะค่าของ s,t,t+r จะถูกตัดทศนิยมออก ซึ่งค่าอาจจะไม่ตรงตามที่ต้องการหากต้องการทศนิยมด้วย
// k) Run ได้แต่ค่าอาจไม่ตรงตามที่ต้องการ เพราะค่าของ s,t,t+r จะถูกตัดทศนิยมออก ซึ่งค่าอาจจะไม่ตรงตามที่ต้องการหากต้องการทศนิยมด้วย
double functionM(){
return 10.55;
}

int functionN(int n, double x){
return 20;
}

double functionO(double a,int b, double c,int d){
return 30.33;
}

double functionP(int a, int n, int b, int c){
return 20.22;
}
